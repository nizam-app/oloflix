// Flutter imports:
import 'package:flutter/material.dart';

class MovieDetails extends StatelessWidget {
  const MovieDetails({super.key});
  static final routeName = "/movie_details";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // endDrawer: AppDrawer(),
    );
  }
}
